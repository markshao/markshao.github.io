<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title><![CDATA[2019 生活 OKR]]></title>
    <url>%2F2019%2F03%2F08%2F2019-objectives%2F</url>
    <content type="text"><![CDATA[这周在北京和老板对齐了接下来双月的 okr ，突然发现 2019年已经过去了快1/4了。第一次尝试给自己全年的工作和生活定一个目标，记录在博客也是希望可以鞭策和督促自己，等年底的时候再来看看完成的怎么样。 技术 学习 Hadoop / MapRedcue / Flink /Kaka 相关的学习，这个也是接下来工作的主要内容 Linux Perfomance 相关知识的深入学习 Chao Engineering 的序诶下 把 Golang 系统性学习一下，争取用 Golang 写一些小东西 分享 JMeter 和 Pytest 的源码分析，其实这块代码之前也断断续续研究过好一阵了，就是没系统性的记录下来 争取可以给 Pytest 提及一个 patch 生活 完成两次国内，一次国外的旅行 （和媳妇一起） 带爸妈去一次北京，弥补一下老爸从来没去过首都的梦想 每周给爸妈打至少3次电话 结婚周年大庆一下 每周保证至少 3 次去健身房锻炼身体，2018 年对于身体的重视太少了，导致身体上开始出现一些小毛病 坚持每天吃早饭 看 15 人文历史方面的书 （先把之前双十一屯的一些书看完吧） 继续坚持博客的写作, 每周至少2篇，记录和分享生活中的点点滴滴 下图拍摄于景山山顶 (2019/3/3)]]></content>
      <categories>
        <category>杂谈</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[JMeter 源码解读 [7] - JMeter 线程模型]]></title>
    <url>%2F2019%2F03%2F08%2Fjmeter-thread-model%2F</url>
    <content type="text"><![CDATA[JMeter 的线程管理主要是通过两个类来进行，一个是ThreadGroup 顾名思义整个是一个线程的集合，另外一个是JMeterThread 整个可以理解为是一个工作线程，用来做实际请求发送的工作。上一次分析StandardJMeterEnginet提到它最后实际是构造了一个ThreadGroup的对象，然后通过调用startThreadGroup方法来启动线程组，实际内部就调用ThreadGroup自身的start方法，那么我们来看一下这个方法的核心在做什么 ThreadGroup.start123456789101112131415delayedStartup = isDelayedStartup(); // Fetch once; needs to stay constant log.info("Starting thread group... number=&#123;&#125; threads=&#123;&#125; ramp-up=&#123;&#125; perThread=&#123;&#125; delayedStart=&#123;&#125;", groupNumber, numThreads, rampUpPeriodInSeconds, perThreadDelayInMillis, delayedStartup); if (delayedStartup) &#123; threadStarter = new Thread(new ThreadStarter(notifier, threadGroupTree, engine), getName()+"-ThreadStarter"); threadStarter.setDaemon(true); threadStarter.start(); // N.B. we don't wait for the thread to complete, as that would prevent parallel TGs &#125; else &#123; long now = System.currentTimeMillis(); // needs to be same time for all threads in the group final JMeterContext context = JMeterContextService.getContext(); for (int threadNum = 0; running &amp;&amp; threadNum &lt; numThreads; threadNum++) &#123; startNewThread(notifier, threadGroupTree, engine, threadNum, context, now, (int)(threadNum * perThreadDelayInMillis)); &#125; &#125; 首先是获取一个delayedStartup 的状态，这个表示的启动线程组的方式，是否是立即启动全部，还是说通过一个period 的周期来启动整个线程组 如果delayedStartup是true 的话，那么就会先启动过一个叫ThreadStarter 的线程，通过它来启动过线程组 反之就直接通过循环调用startNewThread 这个函数的方式来启动线程组 ###看一下 ThreadStarter 线程是怎么启动线程组的 我们知道JMeter在启动线程组的时候是有设置Ramp up period的模式，整个就是通过ThreadStarter线程来实现的，我么简单看一下它的run() 函数是咋么工作的 12345678910final int perThreadDelayInMillis = Math.round((float) (getRampUp() * 1000) / (float) numThreads);for (int threadNumber = 0; running &amp;&amp; threadNumber &lt; numThreads; threadNumber++) &#123; if (threadNumber &gt; 0) &#123; pause(perThreadDelayInMillis); // ramp-up delay (except first) &#125; if (usingScheduler &amp;&amp; System.currentTimeMillis() &gt; endtime) &#123; break; // no point continuing beyond the end time &#125; JMeterThread jmThread = makeThread(notifier, threadGroupTree, engine, threadNumber, context); jmThread.setInitialDelay(0); // Already waited 首先计算一下每个线程启动的时间等待间隔，final int perThreadDelayInMillis = Math.round((float) (getRampUp() * 1000) / (float) numThreads); 然后就是每次启动一个线程的时候，等待一下 pause(perThreadDelayInMillis) 我们再看一下startNewThread 是怎么启动线程的12345678910private JMeterThread startNewThread(ListenerNotifier notifier, ListedHashTree threadGroupTree, StandardJMeterEngine engine, int threadNum, final JMeterContext context, long now, int delay) &#123; JMeterThread jmThread = makeThread(notifier, threadGroupTree, engine, threadNum, context); scheduleThread(jmThread, now); // set start and end time jmThread.setInitialDelay(delay); Thread newThread = new Thread(jmThread, jmThread.getThreadName()); registerStartedThread(jmThread, newThread); newThread.start(); return jmThread; &#125; 非常直观，创建一个JMeterThread 对象，做一些设置，然后放到一个Thread 里面跑就好了，registerStartedThread(jmThread, newThread); 这个调用是在ThreadGroup的一个Map 里把JMeterThread和真实的Thread关系存一下，方便后面的查找 JMeterThread最后我们来看一下核心的JMeterThread 工作线程是怎么work的。首先我们要找一个答案，它怎么处理‘Loop Count / Forever’ 的运行模式的 我之前在设计依图自身的性能测试框架的时候对快问题采用的设计方式是通过producer / consumer的方式把系统解耦，通过一个 Queue来出来，所以对于JMeter的处理方式很好奇。看了代码发现 JMeter的实现非常简答，我们看一下run 函数头部的几个循环判断条件就能找到答案了 123456789101112public void run() &#123; // threadContext is not thread-safe, so keep within thread JMeterContext threadContext = JMeterContextService.getContext(); LoopIterationListener iterationListener = null; try &#123; iterationListener = initRun(threadContext); while (running) &#123; Sampler sam = threadGroupLoopController.next(); while (running &amp;&amp; sam != null) &#123; processSampler(sam, null, threadContext); threadContext.cleanAfterSample(); Sampler 表示一次请求，每次通过threadGroupLoopController.next() 这个迭代器来获取下一个Sampler对象，当 sampler 不为空的时候就继续发请求 threadGroupLoopController 这个对象实际是一个Controller接口的具体实现, Controller 的实现有很多类型，我们可以在界面上选择，也可以在配置文件里面修改, 下面就是用了一个 LoopController 1&lt;elementProp name="ThreadGroup.main_controller" elementType="LoopController" guiclass="LoopControlPanel" testclass="LoopController" testname="Loop Controller" enabled="true"&gt; 当controller对象的isDone判定为真的时候，就把running状态设置成False，那么这个JMeterThread 也就结束生命了 1234if (threadGroupLoopController.isDone()) &#123; running = false; log.info(&quot;Thread is done: &#123;&#125;&quot;, threadName); &#125; 线程设计模型的更多思考参考之前Gatling的使用来看，它对于并发模型本身的支持粒度非常细，例如支持恒定并发，恒定 QPS 等不同并发场景的设计 我在自己设计 Atom Integraion的时候也希望这个框架能在并发模型支持的灵获性上做的更好，通过引入Queue 把 Scheduler 和 Load Generator 分离，我就可以通过写入 Queue 的速度来控制最终 Load Generator 的并发模型 从以前使用 JMeter的经验来看，它对于并发模型的支持是比较有限的，从它目前的设计来看，整个系统的解耦做的是不太好的，首先每个JMeterThread的工作是独立的 , 虽然它们是共享同一个threadGroupLoopController对象，但是通过一个迭代器来控制并发的粒度，实在是有点难，不知道未来JMeter 开发团队对于并发模型支持这块会不会有更多的计划 。 这里只是我的一些愚见， 也欢迎大家留言和我讨论。]]></content>
      <categories>
        <category>JMeter 源码解读</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[JMeter 源码解读 [6] - 核心类 org.apache.jmeter.engine.StandardJMeterEngine]]></title>
    <url>%2F2019%2F03%2F07%2FStandardJMeterEngine%2F</url>
    <content type="text"><![CDATA[我们越来约越接近 JMeter 的核心，先来看一下StandardJMeterEngine 这个类实现了哪些接口，首先是JMeterEngine, 它定义了 JMeterEngine 的一些核心接口 ，其中最主要的是下面几个 123456789101112131415161718/** * Configure engine * @param testPlan the test plan */ void configure(HashTree testPlan); /** * Runs the test * @throws JMeterEngineException if an error occurs */ void runTest() throws JMeterEngineException; /** * Stop test immediately interrupting current samplers */ default void stopTest() &#123; stopTest(true); &#125; 它的主要实现方，一个是单机版本的StandardJMeterEngine , 另一个是分布式模式下的ClientJmeterEngine 另外一个实现的接口是Runnable ，这个很显然我们可以猜到，StandardJMeterEngine 自身会是一个多线程执行的模式，果不其然，我们看到StandardJMeterEngine的runTest()的实现，就是把自己this 放到一个Thread 里来调用 1234567891011121314@Overridepublic void runTest() throws JMeterEngineException &#123; if (host != null)&#123; long now=System.currentTimeMillis(); System.out.println("Starting the test on host " + host + " @ "+new Date(now)+" ("+now+")"); // NOSONAR Intentional &#125; try &#123; Thread runningThread = new Thread(this, "StandardJMeterEngine"); runningThread.start(); &#125; catch (Exception err) &#123; stopTest(); throw new JMeterEngineException(err); &#125;&#125; 我们再来看一下run()的这个Runnable 接口的实现是怎么样的, 首先看到第一行代码 log.info(&quot;Running the test!&quot;); 哈哈，总算到这里时真的开始执行case 了，哎，这一层层的调用，太艰辛了！！！ 首先看一下JMeterContextService.startTest(); 这个是用来设置一个运行启动的状态，JMeterContextService 从名字来看，是一个上下文的类，它是在整个JMeter的线程之间是共享的，除了有一个threadContext 的变量，整个是一个ThreadLocal的独享，是每个线程独享的，我们简单看一些JMeterContextService 就能更清晰它的作用了 1234567891011121314public final class JMeterContextService &#123; private static final ThreadLocal&lt;JMeterContext&gt; threadContext = ThreadLocal.withInitial(JMeterContext::new); //@GuardedBy(JMeterContextService.class) private static long testStart = 0; //@GuardedBy(JMeterContextService.class) private static int numberOfActiveThreads = 0; //@GuardedBy(JMeterContextService.class) private static int numberOfThreadsStarted = 0; //@GuardedBy(JMeterContextService.class) private static int numberOfThreadsFinished = 0; 再继续做了一些简答的初始化后，会先开始做用setup thread group 做一些初始化的工作 ，这里会启动 N 个线程组来做初始化， 1234567891011121314151617181920212223if (setupIter.hasNext()) &#123; log.info("Starting setUp thread groups"); while (running &amp;&amp; setupIter.hasNext()) &#123;//for each setup thread group AbstractThreadGroup group = setupIter.next(); groupCount++; String groupName = group.getName(); log.info("Starting setUp ThreadGroup: &#123;&#125; : &#123;&#125; ", groupCount, groupName); startThreadGroup(group, groupCount, setupSearcher, testLevelElements, notifier); if (serialized &amp;&amp; setupIter.hasNext()) &#123; log.info("Waiting for setup thread group: &#123;&#125; to finish before starting next setup group", groupName); group.waitThreadsStopped(); &#125; &#125; log.info("Waiting for all setup thread groups to exit"); //wait for all Setup Threads To Exit waitThreadsStopped(); log.info("All Setup Threads have ended"); groupCount=0; JMeterContextService.clearTotalThreads(); &#125; groups.clear(); 在 setup thread group 完成一些初始化的工作后，会主动出发一次 JMeterUtils.helpGC(); GC 的动作，后面就是执行测试的Thread Group 和 最后的Post Thread Group ，这些操作和setup thread group 的调用都差不多的，以为实际执行发送请求和并发线程的管理都在 ThreaeGroup 中 今天就说到这里，下次会单独来介绍一下 JMeter 的线程模型]]></content>
      <categories>
        <category>JMeter 源码解读</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[JMeter 源码解读 [5] - 核心类 org.apache.jmeter.JMeter]]></title>
    <url>%2F2019%2F03%2F07%2Fjmeter%2F</url>
    <content type="text"><![CDATA[上一篇 程序入口 NewDriver 提到Main函数是通过反射的方式调用org.apache.jmeter.JMeter 的 start函数来启动Jmeter ，那么今天我们来看看Jmeter 这个类是怎么工作的. 我们从start 函数来开始分析，由于外层的NewDriver 可以看做一层透明的代理，实际的业务逻辑是透传到JMeter来处理的，首先就是解析启动时候传入的外部命令行参数 1234567891011CLArgsParser parser = new CLArgsParser(args, options); String error = parser.getErrorString(); if (error == null)&#123;// Check option combinations boolean gui = parser.getArgumentById(NONGUI_OPT)==null; boolean nonGuiOnly = parser.getArgumentById(REMOTE_OPT)!=null || parser.getArgumentById(REMOTE_OPT_PARAM)!=null || parser.getArgumentById(REMOTE_STOP)!=null; if (gui &amp;&amp; nonGuiOnly) &#123; error = "-r and -R and -X are only valid in non-GUI mode"; &#125; &#125; 我们看到上面的代码的主要工作是解析命令行参数，并且判断有没有存在矛盾的组合，例如GUI 相关的参数，但是实际的运行模式是非GUI 模式等。 各种相关解析的任务完成后，有一个比较重要的工作updateClossLoader的调用，它是JMeter类自身的一个方法，我们可以来看一下 12345private void updateClassLoader() throws MalformedURLException &#123; updatePath("search_paths",";", true); //$NON-NLS-1$//$NON-NLS-2$ updatePath("user.classpath",File.pathSeparator, true);//$NON-NLS-1$ updatePath("plugin_dependency_paths",";", false);//$NON-NLS-1$ &#125; 它的实际作用是把search_paths和user.classpath里面设置的 jar 文件添加到自定义的classloader 中，它的作用可以让我们在某些场景下更方便使用JMeter，例如我们自定义的Java Sampler，在官方的guide里说是要把Java Sampler 打包成 Jar 放到lib/ext 目录下，这个很麻烦，实际上我们可以通过设置search_paths 和user.classpath的方式来简化操作。 然后我们再往下看，跳过一些处理特定option的code ，最核心的就是如下两个核心调用，一个是启动 GUI 的模式, 这里传入的testFile 参数就是我们的 jmx 测试 plan 文件路径 12startGui(testFile);startOptionalServers(); 另外一种就是非 GUI 模式 12startNonGui(testFile, jtlFile, rem, reportAtEndOpt != null);startOptionalServers(); startOptionalServers() 是一个独立的BeanShell server, 用来方便对 Jmeter 进程做一些动态调整，以后可以单独写一个来介绍beanshell server 相关的东西, 这里先不关注了。GUI 的模式在启动过后就能看到界面了（一个 swing 的 app）等待用户的输入，所以我们先看一下startNonGui 这个方法, 这个方法的核心是创建了一个新的JMeter driver (源码里自己也有一个疑问，why does it create a new instance?) ，在非分布式的场景下，实际最后是调用了 123456private void startNonGui(String testFile, String logFile, CLOption remoteStart, boolean generateReportDashboard) throws IllegalUserActionException, ConfigurationException &#123;.... // 忽略上面的一些调用，和 Distributed 相关的函数 driver.runNonGui(testFile, logFile, remoteStart != null, remoteHostsString, generateReportDashboard); &#125; 那么我们再来看runNonGui 在干什么，它是真的要开始跑性能测试了，首先是对于 TestPlan 的 HashTree 的解析，获取需要的相关信息和再做一些初始化，如何解析HashTree这个我们在之前的 HashTree 中已经介绍过了, 最后的核心是调用如下代码来启动 JMeter 测试 12345678910111213141516171819202122if (!remoteStart) &#123; JMeterEngine engine = new StandardJMeterEngine(); engine.configure(clonedTree); long now=System.currentTimeMillis(); println("Starting the test @ "+new Date(now)+" ("+now+")"); engine.runTest(); engines.add(engine); &#125; else &#123; java.util.StringTokenizer st = new java.util.StringTokenizer(remoteHostsString, ",");//$NON-NLS-1$ List&lt;String&gt; hosts = new LinkedList&lt;&gt;(); while (st.hasMoreElements()) &#123; hosts.add((String) st.nextElement()); &#125; DistributedRunner distributedRunner=new DistributedRunner(this.remoteProps); distributedRunner.setStdout(System.out); // NOSONAR distributedRunner.setStdErr(System.err); // NOSONAR distributedRunner.init(hosts, clonedTree); engines.addAll(distributedRunner.getEngines()); distributedRunner.start(); &#125; startUdpDdaemon(engines) 我们看到首先会区分是不是 Remote 模式，我们这里只看非Remote的场景。engine.runTest() 这个是一个异步的调用，实际是启动一个线程，这个具体下次谈StandardJmeterEngine 的时候来看 startUdpDdaemon(engines) 是一个JMeter管理服务，用来管理 Jmeter 的运行状况，例如Shutdown/StopTestNow/HeapDump/ThreadDump 等，这个样的设计让整个JMeter的管理和JMeter自身的进程实现了解耦。 好，下一次我们来看一下 StandardJMeterEngine的实现]]></content>
      <categories>
        <category>JMeter 源码解读</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[JMeter 源码解读 [4] - HashTree]]></title>
    <url>%2F2019%2F03%2F06%2Fhashtree%2F</url>
    <content type="text"><![CDATA[HashTree首先要特别说明一下这里说的HashTree 和我们在数据结构里面谈的 哈希树 完全不是一个概念，一开始我也被它的名字误导，特地去看了一会哈希树相关的技术细节，后来越深人越发现，这个HashTree 完全是挂羊头卖狗肉嘛，不过作为整个JMeter 最核心的一个数据结构，还是很值得认真分析一下的 JMX文件我们知道jmx 是JMeter用来描述测试用例的核心文件，它可以用于在GUI 模式下加载整个测试组件，也可以直接运行于Non-GUI的执行模式，它是格式是基于XML 。那么HashTree就是它在内存的一份映射。 我们先来看一份我自定义的JMX 文件，我把一些 xml 节点都 fold 起来了，否则会很长 可以看到这个xml是有一定格式的，他的每一层都只有2个类型的节点 Object 节点 - 代表一个 Test Component HashTree - 一个HashTree 的子节点 那我们来看一下org.apache.jorphan.collections.HashTree 这个类的定义 1234567891011121314151617public class HashTree implements Serializable, Map&lt;Object, HashTree&gt;, Cloneable &#123; private static final long serialVersionUID = 240L; // Used for the RuntimeException to short-circuit the traversal private static final String FOUND = "found"; // $NON-NLS-1$ // N.B. The keys can be either JMeterTreeNode or TestElement protected final Map&lt;Object, HashTree&gt; data; // /** * Creates an empty new HashTree. */ public HashTree() &#123; this(null, null); &#125; ....... 它的核心存储就是一个protected final Map&lt;Object, HashTree&gt; data 的 Map ，然后通过对外提供Map 接口来给上层提供读写的能力。我尝试单步调试了一下，确认ListedHashTree 在内存的结构就是 jmx 文件的映射, ListedHashTre 是HashTree的子类，实际使用的都是ListedHashMap，它类似于LinkedHashMap ，这里就不展开了 Vistor 设计模式HashTree在数据节点的遍历上还用了一个经典的访问者的设计模式，由于在测试执行中JMeter的Engine需要经常访问 JMX 中的某些节点和子节点，如果把这些方法放在HashTree 本身会使得耦合特别紧密，通过访问者模式可以解耦并提供更好灵活的扩展性, 访问者模式参考这里 HashTreeIterator 定义类访问者的标准接口，类似accept(Object node) 这样的定义，这里主要是addNode的方法，我们看一下HashTreeIterator的定义 12345678910111213141516171819202122232425262728public interface HashTreeTraverser &#123; /** * The tree traverses itself depth-first, calling addNode for each object it * encounters as it goes. This is a callback method, and should not be * called except by a HashTree during traversal. * * @param node * the node currently encountered * @param subTree * the HashTree under the node encountered */ void addNode(Object node, HashTree subTree); /** * Indicates traversal has moved up a step, and the visitor should remove * the top node from its stack structure. This is a callback method, and * should not be called except by a HashTree during traversal. */ void subtractNode(); /** * Process path is called when a leaf is reached. If a visitor wishes to * generate Lists of path elements to each leaf, it should keep a Stack data * structure of nodes passed to it with addNode, and removing top items for * every &#123;@link #subtractNode()&#125; call. This is a callback method, and should * not be called except by a HashTree during traversal. */ void processPath(); SearchByClass 是访问者的实现，它主要实现了addNode的方法，并且通过泛型定义了需要访问的Object 的类型，我们看下代码 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253545556575859606162public class SearchByClass&lt;T&gt; implements HashTreeTraverser &#123; private final List&lt;T&gt; objectsOfClass = new LinkedList&lt;&gt;(); private final Map&lt;Object, ListedHashTree&gt; subTrees = new HashMap&lt;&gt;(); private final Class&lt;T&gt; searchClass; /** * Creates an instance of SearchByClass, and sets the Class to be searched * for. * * @param searchClass * class to be searched for */ public SearchByClass(Class&lt;T&gt; searchClass) &#123; this.searchClass = searchClass; &#125; /** * After traversing the HashTree, call this method to get a collection of * the nodes that were found. * * @return Collection All found nodes of the requested type */ public Collection&lt;T&gt; getSearchResults() &#123; // TODO specify collection type without breaking callers return objectsOfClass; &#125; /** * Given a specific found node, this method will return the sub tree of that * node. * * @param root * the node for which the sub tree is requested * @return HashTree */ public HashTree getSubTree(Object root) &#123; return subTrees.get(root); &#125; /** &#123;@inheritDoc&#125; */ @SuppressWarnings("unchecked") @Override public void addNode(Object node, HashTree subTree) &#123; // 某一层HashTre 的数据访问 if (searchClass.isAssignableFrom(node.getClass())) &#123; objectsOfClass.add((T) node); ListedHashTree tree = new ListedHashTree(node); tree.set(node, subTree); subTrees.put(node, tree); &#125; &#125; /** &#123;@inheritDoc&#125; */ @Override public void subtractNode() &#123; &#125; /** &#123;@inheritDoc&#125; */ @Override public void processPath() &#123; &#125;&#125; 我们看一下业务代码怎么使用访问者模式来选择 jmx 中的某个子节点，这里借用核心的StandardJMeterEnginer的configure函数来说明一下 123456789101112131415@Override public void configure(HashTree testTree) &#123; // Is testplan serialised? SearchByClass&lt;TestPlan&gt; testPlan = new SearchByClass&lt;&gt;(TestPlan.class); testTree.traverse(testPlan); Object[] plan = testPlan.getSearchResults().toArray(); if (plan.length == 0) &#123; throw new IllegalStateException("Could not find the TestPlan class!"); &#125; TestPlan tp = (TestPlan) plan[0]; serialized = tp.isSerialized(); tearDownOnShutdown = tp.isTearDownOnShutdown(); active = true; test = testTree; &#125; 目标是要获取 TestPlan 的节点，我们看怎么调用的 创建SearchByClass对象，传入TestPlan.class 用来构造 调用 HashTree 资深的 traverse 方法，传入SearchByClass 对象 通过 testPlan.getSearchResults().toArray(); 获取访问结果 关于HashTree就说到这了，下次我们深入到JMeter 的运行原理去进一步分析]]></content>
      <categories>
        <category>JMeter 源码解读</category>
      </categories>
      <tags>
        <tag>设计模式</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[JMeter 源码解读 [3] - 程序入口 NewDriver]]></title>
    <url>%2F2019%2F03%2F05%2Fjmeter-new-driver%2F</url>
    <content type="text"><![CDATA[NewDriver 的完整路径是org.apache.jmeter.NewDriver , 它是整个JMeter 的入口类，主要的作用是提供了main 函数用于启动JMeter. 不过在启动程序 main 函数之前，NewDriver会做一些运行环境的检查和初始化，主要通过 static initializer 的方式来做，我们先来看一些代码初始化的代码 12345678910111213141516171819202122232425262728293031private static final String JAVA_CLASS_PATH = "java.class.path";// $NON-NLS-1$// 其他省略了一些代码不贴这里了，否则太长了// 这个就是静态初始化的代码块static &#123; final List&lt;URL&gt; jars = new LinkedList&lt;&gt;(); final String initiaClasspath = System.getProperty(JAVA_CLASS_PATH); // Find JMeter home dir from the initial classpath String tmpDir; StringTokenizer tok = new StringTokenizer(initiaClasspath, File.pathSeparator); if (tok.countTokens() == 1 || (tok.countTokens() == 2 // Java on Mac OS can add a second entry to the initial classpath &amp;&amp; OS_NAME_LC.startsWith("mac os x")// $NON-NLS-1$ ) ) &#123; File jar = new File(tok.nextToken()); try &#123; tmpDir = jar.getCanonicalFile().getParentFile().getParent(); &#125; catch (IOException e) &#123; tmpDir = null; &#125; &#125; else &#123;// e.g. started from IDE with full classpath tmpDir = System.getProperty("jmeter.home","/Users/ylshao/code/github/jmeter");// Allow override $NON-NLS-1$ $NON-NLS-2$ if (tmpDir.length() == 0) &#123; File userDir = new File(System.getProperty("user.dir"));// $NON-NLS-1$ tmpDir = userDir.getAbsoluteFile().getParent(); &#125; &#125; JMETER_INSTALLATION_DIRECTORY=tmpDir; // 获取 jmeter 当前安装目录 这个是静态初始化代码的一部分，我们看到主要的作用是判断 JMeter 的安装目录，对于通过jmeter.sh 运行的方式是通过 classpath 来推导 parent ，否则就通过读取系统变量 jmeter.home 来获取，最后把tmpDir 赋值给 JMETER_INSTALLATION_DIRECTORY 变量，JMETER_INSTALLATION_DIRECTORY 这个变量还是很很重要的，用来推导整个jmeter 后续的目录结构, 看下面的代码 12345// Add standard jar locations to initial classpath StringBuilder classpath = new StringBuilder(); File[] libDirs = new File[] &#123; new File(JMETER_INSTALLATION_DIRECTORY + File.separator + "lib"),// $NON-NLS-1$ $NON-NLS-2$ new File(JMETER_INSTALLATION_DIRECTORY + File.separator + "lib" + File.separator + "ext"),// $NON-NLS-1$ $NON-NLS-2$ new File(JMETER_INSTALLATION_DIRECTORY + File.separator + "lib" + File.separator + "junit")&#125;;// $NON-NLS-1$ $NON-NLS-2$ 通过上面计算出来的JMETER_INSTALLATION_DIRECTORY,可以获取相关 lib 和其子目录的路径，并读取到他们下面所有的jar 文件，最后的目的是通过这些 jar 文件创建一个 classloader 123456789101112131415161718192021222324252627282930for (File libJar : libJars) &#123; try &#123; String s = libJar.getPath(); // Fix path to allow the use of UNC URLs if (usesUNC) &#123; if (s.startsWith("\\\\") &amp;&amp; !s.startsWith("\\\\\\")) &#123;// $NON-NLS-1$ $NON-NLS-2$ s = "\\\\" + s;// $NON-NLS-1$ &#125; else if (s.startsWith("//") &amp;&amp; !s.startsWith("///")) &#123;// $NON-NLS-1$ $NON-NLS-2$ s = "//" + s;// $NON-NLS-1$ &#125; &#125; // usesUNC jars.add(new File(s).toURI().toURL());// See Java bug 4496398 classpath.append(CLASSPATH_SEPARATOR); classpath.append(s); &#125; catch (MalformedURLException e) &#123; // NOSONAR EXCEPTIONS_IN_INIT.add(new Exception("Error adding jar:"+libJar.getAbsolutePath(), e)); &#125; &#125; &#125; // ClassFinder needs the classpath System.setProperty(JAVA_CLASS_PATH, initiaClasspath + classpath.toString()); // ### 创建自定义的classloader loader = AccessController.doPrivileged( (PrivilegedAction&lt;DynamicClassLoader&gt;) () -&gt; new DynamicClassLoader(jars.toArray(new URL[jars.size()])) );&#125; 直到创建了 loader 这个自定义的 classloader 后，整个静态初始化才算结束 最后我们来看一些 main 函数，逻辑就比较简单了 123456789101112131415161718192021222324public static void main(String[] args) &#123; if(!EXCEPTIONS_IN_INIT.isEmpty()) &#123; System.err.println("Con figuration error during init, see exceptions:"+exceptionsToString(EXCEPTIONS_IN_INIT)); // NOSONAR Intentional System.err use &#125; else &#123; Thread.currentThread().setContextClassLoader(loader); setLoggingProperties(args); try &#123; // Only set property if it has not been set explicitely if(System.getProperty(HEADLESS_MODE_PROPERTY) == null &amp;&amp; shouldBeHeadless(args)) &#123; System.setProperty(HEADLESS_MODE_PROPERTY, "true"); &#125; Class&lt;?&gt; initialClass = loader.loadClass("org.apache.jmeter.JMeter");// $NON-NLS-1$ Object instance = initialClass.getDeclaredConstructor().newInstance(); Method startup = initialClass.getMethod("start", new Class[] &#123; new String[0].getClass() &#125;);// $NON-NLS-1$ startup.invoke(instance, new Object[] &#123; args &#125;); &#125; catch(Throwable e)&#123; // NOSONAR We want to log home directory in case of exception e.printStackTrace(); // NOSONAR No logger at this step System.err.println("JMeter home directory was detected as: "+JMETER_INSTALLATION_DIRECTORY); // NOSONAR Intentional System.err use &#125; &#125; &#125; 先判断一下EXCEPTIONS_IN_INIT 是否为空，如果不为空就表示静态初始化有异常，直接退出进程并打印错误信息 初始化一些基本的日志配置 用刚才初始化的 classloader 加载类 org.apache.jmeter.JMeter 然后通过 java 反射的方式来调用 org.apache.jmeter.JMeter 的 start 方法，正式完成 JMeter 的启动]]></content>
      <categories>
        <category>JMeter 源码解读</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[JMeter 源码解读 [2] - 目录结构分析]]></title>
    <url>%2F2019%2F03%2F05%2Fjmeter-directory-structure%2F</url>
    <content type="text"><![CDATA[本来是准备自己写一个关于Jmeter 目录结构的分析，碰巧看到了这篇 官方文档 内容基本和我要写的差不都，就直接拿来翻译记录一下（对于其中一些过时的内容会直接删除掉） JMeter 的代码目录主要是根据支持的协议和功能来组织的。这样设计的好处是开发者可以编译一个只支持某种协议的JMeter 包而不用编译整个应用。我们接下来会给到更多JMeter 代码目录的解析，因为大多数的JMeter开发者是用ecipse来做开发环境，我们也用eclipse 的目录结构来做介绍 顶部目录 bin - 包含.bat 和 . sh 这些文件用于启动JMeter。同时也包含了ApacheJmeter.jar 和 相关的配置文件 build - build 脚本创建的目录，存放一些build过程生成的文件 dist - build 脚本创建饿目录，存放最后输出的文件 docs - JMeter 相关目录 extras - ant 相关的其他文件 lib - 包含JMeter 依赖的相关文件 src - 子目录包含支持的协议和相关组件代码 test - 单元测试目录 xdocs - 用于生成文档的xml文件，JMeter用xml来生成文档 Lib 目录结构 ext - 包含 JMeter 核心和协议相关的 jar 文件。把这些 jar 文件独立出来原因是如果都放在lib/目录里启动速度会变慢 opt - 一些可选的 jar 用于 JMeter 的一些可选的功能。这些 jar 只有在build 和 运行的时候才会被引入。用户可以自己下来可选的 jar 放到整个目录里 Src 目录结构 core - JMeter 核心功能和接口的代码目录，也是我们分析的重点 components - 和协议无关的一些类，例如协议， GUI 组件等 examples - 一些sampler的例子 functions - 一些其他组件会使用的标准函数 jorphan - 公共方法类 （https://markmail.org/message/xisc5fw2dr4bw4uy） protocol - 不同的协议支持代码 junit - 测试相关的代码]]></content>
      <categories>
        <category>JMeter 源码解读</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[vscode 杂谈]]></title>
    <url>%2F2019%2F03%2F04%2Fvscode-introduction%2F</url>
    <content type="text"><![CDATA[在依图的时候为了安利周围的同学都使用visual studio code ，特地写了一个ppt给他们做了一个培训。当然不是介绍vscode 本身那么无聊，主要是八卦了一下vscode 背后的作者以及相关的技术实现。 vscode 本身的设计还是很经典的, 主要是2个方面 Plugin 解偶，不同于过去 eclipse OSGI 的全部加载到内存模式，通过 RPC 和一个独立于vscode 的n extension进程进行通信的方式来工作 [Language Server Protoocl][https://langserver.org/] ，整个设计非常优雅，例如Java Extension 其实就没有从头开始开发，基于eclipse 封装了一个Language Server 就完事了 有兴趣的同学可以看一下PPT ，里面还介绍了vscode 的作者，都是一堆大牛]]></content>
      <categories>
        <category>杂谈</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[JMeter 源码解读 [1] - 配置开发环境]]></title>
    <url>%2F2019%2F03%2F03%2Fjmeter-source-code-initenviroment%2F</url>
    <content type="text"><![CDATA[准备环境 操作系统 MacOS Mojave JDK 1.8 Git Apache Ant (Mac 的化可以用 brew install ant 的方式来安装)， 版本用最新的就好了 Intellij 个人认为最好的Java IDE, 下载一个最新的版本就好了 clone代码到本地目录下1&gt; git clone git@github.com:apache/jmeter.git ###进入到jmeter目录，下载第三方依赖 Jar 1&gt; ant download_jars 等download_jars任务完成后，确认一下lib目录下已经有了需要的依赖Jar 文件了 ###用 Intellij 直接打开 JMeter 如果之前安装intellij 时已经把idea添加到了command line的可以，可以在Jmeter目录下直接通过命令行打开 1&gt; idea . ###配置 IDE 添加 Jar 到 classpath ，点击右上角的蓝色按钮，选择Project Settings -&gt; Library,选择+ ，并选择Java, 然后选择lib后点击确认, 这样就把之前download_jars下载到lib中的所有jar添加到了classpath 中 配置环境变量Jmeter 的 Main函数入口在org.apache.jmeter.NewDriver这个class，在完成 classpath 设置后就可以直接运行，但是会报很多Runtime的Exception，我们需要再做一些设置 设置 jmeter.properties. 如果是用ant打包的话会把相关的resource 都打到Jar包内，但是如果通过源码来运行的话，需要显示指定一下jmeter.properties 否则会报错，提示找不到配置文件。方法就是修改运行的配置，增加一个命令行参数，手动设置jmeter.propreties, 在这里我用的是绝对路径 修改一下jmeter.properties, 把第37行注释打开就好，否则系统界面会是一堆乱码的界面，这里我选择的方式是改成英文就好了 1language=en 最后改动一下org.apache.jmeter.NewDriver中第83行代码，设置一下当前jmeter目录的绝对路径 (之前想用System Property来设置，好像没什么小姑哦，就直接改源代码好了) 123456789101112131415161718if (tok.countTokens() == 1 || (tok.countTokens() == 2 // Java on Mac OS can add a second entry to the initial classpath &amp;&amp; OS_NAME_LC.startsWith("mac os x")// $NON-NLS-1$ ) ) &#123; File jar = new File(tok.nextToken()); try &#123; tmpDir = jar.getCanonicalFile().getParentFile().getParent(); &#125; catch (IOException e) &#123; tmpDir = null; &#125; &#125; else &#123;// e.g. started from IDE with full classpath tmpDir = System.getProperty("jmeter.home","/Users/ylshao/code/github/jmeter");// Allow override $NON-NLS-1$ $NON-NLS-2$ if (tmpDir.length() == 0) &#123; File userDir = new File(System.getProperty("user.dir"));// $NON-NLS-1$ tmpDir = userDir.getAbsoluteFile().getParent(); &#125; &#125; 大功告成好了，现在启动一下org.apache.jmeter.NewDriver 这个入口程序，就可以看到熟悉的Jmeter 界面了 问题解答 过程中如果遇到一些test目录提示编译失败的化，可以直接在Project Settings - Module中把他们删除掉就好了，反正源码分析暂时也用不到]]></content>
      <categories>
        <category>JMeter 源码解读</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[2018 总结 [质量篇]]]></title>
    <url>%2F2019%2F03%2F01%2F2018-conclusion%2F</url>
    <content type="text"><![CDATA[上一次写博客已经不知道是多久之前的事情了，开通 Github Page 刚开始更多是出于对于新事物尝鲜，对于坚持写作这个事情反而没有怎么重视。工作这几年，特别是在依图的这2年越来越感受到思考的深度是多么的重要，而其中写作正好是一个非常好的静下来去思考，去复盘的一个手段。趁着这段时间在北京出差在酒店空着没事儿，把之前基于octopress 的 blog 迁移到了 hexo , 希望从2019年开始，通过这里记录下工作生活中的点点滴滴，最关健的是要能一直坚持下去。 关于工作 整个2018年的工作可以主要分为两个阶段，上半年我基本上是戴着测试团队负责人的帽子干着 60% 测试的工作和 40% 产品经理的工作，下半年是戴着产品经理的帽子干着 80% 产品管理的活和 20% 测试横向的事情 。 先来谈谈在测试这块做的一些工作，记录一些关于框架，工具方面的沉淀吧 Atom Integration这个是我上半年在负责原子的测试的时候设计开发的一个性能测试框架，至于为什么我会去负责原子的测试，主要的原因是2个 整个2017年的招聘工作不给力，18年纵队化改革后，一下子暴露出了人员短缺的问题，当时的主力都要投到人像大平台和人像大数据的产品上，作为公司新的重要孵化产品，只能我自己上了 另外原子是一个全新的贴近算法的层面的底层产品，支持分布式，我个人也比较希望可以借这个机会对底层的一些技术可以有更多的了解，而且当时团队都是Python，就我可能算还会一些一点 Java 原子作为一个底层算法的产品，最为关注的就是性能，由于对外提供的 interface 是Java SDK （RPC Client），所以我一开始选择的方式的 Jmeter + Java Sampler的方式。但是由于我们的Client 中存在一些同步转异步的操作，内部Client的线程模型套在Jmeter的线程模型里后，最后测试出来的QPS 和开发自测的QPS总是不能对齐，而且GPU的利用率也一直上不去，当时项目实在是紧急，也来不及通过看Jmetersource code 的方式来排查具体的原因，就决定通过快速rush 一个性能压测的框架出来再说，取名 Atom Integration, 整个框架基于 Java Executors 的并发模型，支持通过 Console 输出最终的 QPS 和平均 Latency，对于测试过程中的实时 QPS 的 Latency 的监控是通过一个异步的线程把metrics 打到influxdb， 并通过grafana来看，在开发的过程中还是遇到了一些问题，后面我会单独写一篇文章来介绍这个整个设计的思路和过程，这里把当时遇到的问题这里记一下 长时间运行的时候，Result 的 Queue 会出现 Out of memory 的情况 P99 和 P95 计算方式设计不太好，最后用了一个time window的方式，丢掉了一些历史数据 Request 的唯一性问题，因为 Result 是一个HashMap &lt;Request, Latency&gt;, 但是 Request 的唯一性不太够，导致出现 Key Conflict Atom Integration 开发出来后，支持了原子多个版本的性能测试，后来还被他们改造成了一个HTTP Result API 的压力测试框架。这段经历弥足珍贵，让我有机会从头设计一个性能测试框架，对于线程的调度治理，并发模型的控制有了更加深刻的认识 Kan Kan吃狗粮是我们重要的质量文化，但是人像原子作为一个服务器的产品是没有界面的，没法吃狗粮。我花了一个星期的时间做了一个叫KanKan的前端应用，其实这一个星期中的大部分是在学习前端的技术，主要是Web Socket 相关的内容。因为之前在人像大平台和前端聊希望他们引入Web Socket技术来提高drawer的效果被他们以复杂拒绝后，我就一直希望在原子这里可以i 尝试一下Web Socket技术，现在KanKan在富麟他们的改造下（增加了更多监控metraic），已经成为一个重要的稳定性测试工具，关键还挺好玩的 Nebula下半年开始转型产品经理后，也花了一部分时间承担了测试团队横向工作方面的一些事情，最主要的就是CI 2.0的架构设计，也就是Nebula 项目。 目的是希望这个项目可以作为整个团队的横向工作，让大家纵向工作之余的时间可以投入到这个它的开发中去，并且最终可以帮助到纵向的测试工作，提高整体的研发效率. Nebula的整体设计的核心思想就是TaaS - Test as a Service ，希望把测试也作为一个特殊的服务放到整个被测试服务的链路中来。当然它所聚焦的问题就是我们面前80%的测试团队在做的事情，集成测试和性能测试，主要针对的是各种服务，其中HTTP 为主，有一些是RPC 或者是自己的私有协议。希望通过一些工具把这些测试过程中问题统一解决掉，包括被测试服务的部署，接口测试，性能测试等，并结合Docker Kubernetse， Jenkins ，ELK等开源工具，把整个工具链系统化，平台化，服务化。后面有时间也会写一系列文站过来介绍 Nebula 中一些核心组件的设计思想，先放一个脱敏过的 PPT在这里，欢迎有兴趣的同学给我留言讨论]]></content>
      <categories>
        <category>工作感想</category>
      </categories>
      <tags>
        <tag>Nebula</tag>
      </tags>
  </entry>
  <entry>
    <title><![CDATA[浅谈Testng测试框架的分布式实现]]></title>
    <url>%2F2014%2F03%2F01%2Fnew-testng%2F</url>
    <content type="text"><![CDATA[现状分析TestNG是目前在Java领域用的比较多的一款测试框架，它是基于JUnit的一个二次封装，不过增加了很多新的功能，比如完全基于Annotaion的配置，测试用例的依赖等等。你可以通过review它的Guide文档来获得更多的信息，不够有一个问题就是，TestNG的文档里面之字未提关于分布式执行测试用例方面的内容，难道TestNG不支持分布式测试么？错，其实作者老早就写了一篇Blog来介绍如何使用TestNG的分布式了，详情请见TestNG Distribute。但是如果你拿最新的Testng6.8.7的版本来配置分布式的实现，肯定会遇到一堆的问题，所以结论就是目前的分布式是有Bug存在的。 问题在哪里既然有问题，我们就开始尝试从源码的角度来查找问题了。TestNG的源码目前托管在GitHub上,下载下来以后，只要把其中的ivy.jar复制到ant的lib目录下，然后修改一下build.properties的”yaml.jar=snakeyaml-1.6.jar”到”yaml.jar=snakeyaml-1.12.jar”，然后直接ant build就可以编译一个定制化的TestNG了。TestNG和分布式相关的一系列相关code在remote的包里面。经过阅读，我们发现了一些问题。 org.testng.remote.RemoteSuiteWorker 1234567891011121314151617181920public class RemoteSuiteWorker extends RemoteWorker implements Runnable &#123; private XmlSuite m_suite; public RemoteSuiteWorker(XmlSuite suite, SlavePool slavePool, RemoteResultListener listener) &#123; super(listener, slavePool); m_suite = suite; &#125; @Override public void run() &#123; try &#123; SuiteRunner result = sendSuite(getSlavePool().getSlave(), m_suite); m_listener.onResult(result); &#125; catch (Exception e) &#123; e.printStackTrace(); &#125; &#125;&#125; 每一个RemoteSuiteWorker是一个线程，每一个Thread对应一个Slave机器，然后通过Socket通信把一个Xmluite对象发送到Slave端进行执行。TestNG通过一个SlavePool来管理所有的链接，但是我们在这里就能看到一个问题，这个连接池的实现缺少了一个把Socket连接返回到SlavePool的操作，所以说一旦case执行的数量超过了Slave的机器的数量，那么后面一旦SlavePool枯竭了，那么整个分布式执行就会Block在了SlavePool这里了。对了，SlavePool是一个阻塞的双端队列的实现。 org.testng.remote.adapter.DefaultMasterAdapter 12345public void awaitTermination(long timeout) throws InterruptedException &#123; ThreadUtil.execute(m_workers, 1, 10 * 1000L, false); &#125; 当Master进程把所有的测试用例分发到一个队列以后，就需要把这个队列放到一个多线程里面去执行，上面这个方法就是需要去执行所有线程的入口，我们来看一下ThreadUtil.execute的具体实现 1234567891011121314151617181920212223242526272829303132333435363738394041424344454647484950515253/** * Parallel execution of the &lt;code&gt;tasks&lt;/code&gt;. The startup is synchronized so this method * emulates a load test. * @param tasks the list of tasks to be run * @param threadPoolSize the size of the parallel threads to be used to execute the tasks * @param timeout a maximum timeout to wait for tasks finalization * @param triggerAtOnce &lt;tt&gt;true&lt;/tt&gt; if the parallel execution of tasks should be trigger at once */public static final void execute(List&lt;? extends Runnable&gt; tasks, int threadPoolSize, long timeout, boolean triggerAtOnce) &#123; final CountDownLatch startGate= new CountDownLatch(1); final CountDownLatch endGate= new CountDownLatch(tasks.size()); Utils.log("ThreadUtil", 2, "Starting executor timeOut:" + timeout + "ms" + " workers:" + tasks.size() + " threadPoolSize:" + threadPoolSize); ExecutorService pooledExecutor = // Executors.newFixedThreadPool(threadPoolSize); new ThreadPoolExecutor(threadPoolSize, threadPoolSize, timeout, TimeUnit.MILLISECONDS, new LinkedBlockingQueue&lt;Runnable&gt;(), new ThreadFactory() &#123; @Override public Thread newThread(Runnable r) &#123; Thread result = new Thread(r); result.setName(THREAD_NAME); return result; &#125; &#125;); List&lt;Callable&lt;Object&gt;&gt; callables = Lists.newArrayList(); for (final Runnable task : tasks) &#123; callables.add(new Callable&lt;Object&gt;() &#123; @Override public Object call() throws Exception &#123; task.run(); return null; &#125; &#125;); &#125; try &#123; if (timeout != 0) &#123; pooledExecutor.invokeAll(callables, timeout, TimeUnit.MILLISECONDS); &#125; else &#123; pooledExecutor.invokeAll(callables); &#125; &#125; catch (InterruptedException handled) &#123; handled.printStackTrace(); Thread.currentThread().interrupt(); &#125; finally &#123; pooledExecutor.shutdown(); &#125;&#125; 问题就出现了，ThreadUtil.execute(m_workers, 1, 10 * 1000L, false);的调用中设定了线程池的大小为1，而且maxpoolsize = corepoolsize = 1 ,这个问题严重了，所谓的多线程其实是个单线程，这样永远只有一个机器可以执行任务。其实我们只要这样修改就好了 1234public void awaitTermination(long timeout) throws InterruptedException &#123; ThreadUtil.execute(m_workers, m_hosts.length(), 10 * 1000L, false); &#125; 把线程池的大小修改成slave的机器的数量就，这样就解决了线程池的问题。其次，invokeAll这个方法是非阻塞的，这个会导致Master进程会退出，所以的子线程会被丢失，这样的结果一旦子线程完成当前任务，尝试再次连接我们的master服务就会发生问题，从而发生雪崩效应，导致整个分布式集群的crash，看一下下面的slave进程连接master的代码就知道了，这个是个死循环啊！！！12345678910111213141516171819202122public void waitForSuites() &#123; try &#123; while (true) &#123; //TODO set timeout XmlSuite s = m_slaveAdpter.getSuite(Long.MAX_VALUE); if( s== null) &#123; continue; &#125; log("Processing " + s.getName()); List&lt;XmlSuite&gt; suites = Lists.newArrayList(); suites.add(s); m_testng.setXmlSuites(suites); List&lt;ISuite&gt; suiteRunners = m_testng.runSuitesLocally(); ISuite sr = suiteRunners.get(0); log("Done processing " + s.getName()); m_slaveAdpter.returnResult(sr); &#125; &#125; catch(Exception ex) &#123; ex.printStackTrace(System.out); &#125; &#125; 那么怎么办呢，我们知道master其实是把所有的runnable转换成了callable了，那么我们只要遍列所有的callable对象，尝试获取一下结果就好了，因为这里会block我们的master进程，知道最后的退出。 序列化问题(有待解决) sokcet通信里面一个问题是序列化，TestNG的序列化这块非常多的问题，所有的Slave在完成任务以后，会把一个SuiteRunner的对象返回回来，这个SuiteRunner对象可是非常的复杂，深度嵌套而且隐患无数，我们先来看一下下面的代码 12345678910111213141516171819202122232425262728293031323334353637383940public class SuiteRunner implements ISuite, Serializable, IInvokedMethodListener &#123; /* generated */ private static final long serialVersionUID = 5284208932089503131L; private static final String DEFAULT_OUTPUT_DIR = "test-output"; private Map&lt;String, ISuiteResult&gt; m_suiteResults = Collections.synchronizedMap(Maps.&lt;String, ISuiteResult&gt;newLinkedHashMap()); transient private List&lt;TestRunner&gt; m_testRunners = Lists.newArrayList(); transient private List&lt;ISuiteListener&gt; m_listeners = Lists.newArrayList(); transient private TestListenerAdapter m_textReporter = new TestListenerAdapter(); private String m_outputDir; // DEFAULT_OUTPUT_DIR; private XmlSuite m_suite; private Injector m_parentInjector; transient private List&lt;ITestListener&gt; m_testListeners = Lists.newArrayList(); transient private ITestRunnerFactory m_tmpRunnerFactory; transient private ITestRunnerFactory m_runnerFactory; transient private boolean m_useDefaultListeners = true; // The remote host where this suite was run, or null if run locally private String m_host; // The configuration transient private IConfiguration m_configuration; transient private ITestObjectFactory m_objectFactory; transient private Boolean m_skipFailedInvocationCounts = Boolean.FALSE; transient private IMethodInterceptor m_methodInterceptor; private List&lt;IInvokedMethodListener&gt; m_invokedMethodListeners; /** The list of all the methods invoked during this run */ private List&lt;IInvokedMethod&gt; m_invokedMethods = Collections.synchronizedList(Lists.&lt;IInvokedMethod&gt;newArrayList()); private List&lt;ITestNGMethod&gt; m_allTestMethods = Lists.newArrayList();&#125; 这个问题复杂了，我们要么重写整个返回结构，否则我们很可能由于使用的DataProvider的参数不能序列化而导致通信的失败。目前我们采用的方式是完全不返回SuiteRunner ，只是返回一个简单的枚举OK,FAIL,然后通过每个Slave把结果输出到一个共享目录，最终再整合。 今天只是简单的把之前研究TestNG的结果分享在这里，如果你有兴趣觉得我写的东西对你有用，请和我一起祈祷上苍，保佑我奶奶身体健康，长命百岁！！！ 谢谢。]]></content>
      <categories>
        <category>测试框架</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[2013年终小结]]></title>
    <url>%2F2014%2F01%2F27%2F2013-conlusion%2F</url>
    <content type="text"><![CDATA[再过几天就要过春节了，办公室也已经没什么人了，显得有点冷冷清清的。闲着也没什么事情干，就写一下过去一年的小结吧，只是一个简单的流水帐，好歹以后老了可以回顾回顾，否则很多东西都不记得了以后，哈哈. 工作过去的2013年是我在EMC的第二个年头了，从一个新员工慢慢开始过度到老员工的状态了。首先从我的上班时间来看，那是越来越一个晚啊。平均实际每天早上到公司的时间基本固定在了10:30左右了，这个在明年必须要有所改观，用我老爸的话说，你就算他妈的一个天才，每天那么个迟到法，也不会有什么大的成就. 今年主要是围着两个项目在工作 XDB XCP REST API 先来谈谈XDB项目吧，这个应该算是我经历的第一个纯数据库的工作。期间我也是第一次和EMC-IIG的第一QA大牛zhenjie一起合作，那是相当激动。不过在XDB项目的几个月我基本属于一个混日子的，其实基本上的活都是荷兰人干掉了，我花了几个月的时间，把XDB的source code倒是反反复复看了很多遍，也算是提升了一下自己的编码能力吧。 当然咱也不能白干活，否则老板要说我没有output了，这个期间和Alex.Lv一起写了一个名字叫Tangram的工具，其实这个工具的本来名字叫XMNS(XDB MulitNode System)，但是后来考虑到要去北京参加QCon的演讲，这个名字说出来实在是太难听了，我苦思冥想了半天，自以为Tangram的名字算是原创了，搞半天后来发现，其实Baidu的前端team老早用了这个名字了，看来起名字是个技术活. 靠着Tangram，咱也小小出去混了一圈技术圈，完成了人生第一次站上QCon讲台的目标,并且后来也参加了第二届中国软件测试大会 QCON 中国软件测试大会 总结一下参会后的感觉，发现在虚拟化这块，我们还是走的比很多公司要前沿一点，但是也有一些局限，比如我们过多依赖VMWare的产品，导致我们的东西在受众面上相对狭隘了一点，未来再工作中要尝试多研究尝试用一些开源的虚拟化方案，比如Linux Container,OpenStack等等. XCP REST API项目算是我第一次真正意义上来管理一个QA的项目了，当然也要感谢老大一直在从旁辅佐我，还有就是Limonov Dimitry的宽宏大量。在我无数次忘记发meeting request的时候，还好那么友好的对待我，当然还要感谢Lijia和zhiwen，尤其是Lijia，帮我解决了那么多恶心的环境部署的问题，真心想想要是没有Lijia,我真的不敢想象后面的项目会怎么进展下去。未来在项目的风险把控上必须要有所提高和改进。也要改正一些毛病，比如把一些环境搭建的问题完全依赖他人，导致自己在这块的信息完全没有掌握，这个成为了一个很大的弊端。 XCP REST项目中，我也尝试引入了一些新的技术，比如第一次使用了SIKULI,而且这次我们不单单只是只用SIKULI,我们还和SIKULI的社区有非常紧密的交流,我第一次给这个开源项目提交了BUG. 工作就总结到这里吧，争取明年有新的突破 运动足球是今年的主旋律，我最主要的成就是把我们IIG的足球队给组建起来了，而且还参加了第二届VMWare-EMC的夏季足球联赛，虽然我们在第二轮不幸被淘汰，但是这个是我们第一次真正的以IIG的独立身份参加的比赛。而且球员的阵容也是空前的强大和完整，相信明年我们不会让大家失望了。 游泳相对而言有所减弱，没有那么持续的坚持了，明年要有所改善 乒乓球方面我开始尝试使用直拍横打的技术了，目前看效果一般 ，但是华丽程度还是不错的，明年要把这个和我的正手结合起来，冲击一下乒乓球比赛的8强 生活今年把驾照给考出来了，真心不容易啊。开着兄弟的跑车载着yilan出去兜风了一下，感觉还是不错的，哈哈。 12月参加了嬉相记的兄弟们的聚会，大家看来混得都不错，尤其是小潘，完全是一个CEO的状态了，期待以后我们还有机会聚在一起再干一番事业出来吧 展望明年，我希望一切能更好吧，期望我家人和朋友都身体健康，期待我自己明年马上有钱，哈哈。 大家新年快乐！！！]]></content>
      <categories>
        <category>工作感想</category>
      </categories>
  </entry>
  <entry>
    <title><![CDATA[第一篇POST纪念]]></title>
    <url>%2F2014%2F01%2F13%2Ffirst-post%2F</url>
    <content type="text"><![CDATA[今天是2014/1/13日，正式在GITHUB上托管我的Blog，从此记录下我生活工作的点点滴滴。]]></content>
      <categories>
        <category>工作感想</category>
      </categories>
  </entry>
</search>
